[Unit]
Description=httpbeat
Wants=network-online.target
After=network-online.target

[Service]
ExecStart={{ binary_path }} -c {{ redisbeat_configfile_path }}/{{ redisbeat_fileconf_name }}
Restart=always

[Install]
WantedBy=multi-user.target

