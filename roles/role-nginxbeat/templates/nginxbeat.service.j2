[Unit]
Description=httpbeat
Wants=network-online.target
After=network-online.target

[Service]
ExecStart={{ binary_path }} -c {{ nginxbeat_configfile_path }}/{{ nginxbeat_fileconf_name }}
Restart=always

[Install]
WantedBy=multi-user.target

